# PixelKnit - 像素织梦

一个功能完整的 Web 编织引导应用程序，采用模块化架构设计。

## 📁 项目结构

```
apps/PixelKnit/
├── index.html              # 主入口 HTML 文件
├── assets/
│   ├── css/
│   │   └── main.css        # 主样式文件
│   └── js/
│       ├── main.js         # 主入口文件
│       ├── core/           # 核心模块
│       │   ├── MapManager.js      # 地图管理
│       │   ├── GridRenderer.js    # 网格渲染
│       │   ├── Editor.js          # 编辑器
│       │   ├── PathCalculator.js  # 路径计算
│       │   └── ProgressTracker.js # 进度跟踪
│       ├── ui/             # UI组件
│       │   ├── Navigation.js      # 导航栏
│       │   ├── MapList.js         # 地图列表
│       │   ├── Toolbar.js         # 工具栏
│       │   ├── ControlPanel.js    # 控制面板
│       │   ├── Magnifier.js       # 放大镜
│       │   └── ColorPalette.js    # 调色板
│       ├── utils/          # 工具类
│       │   ├── Storage.js         # 数据持久化
│       │   ├── Algorithms.js      # 算法（Bresenham等）
│       │   ├── Helpers.js         # 辅助函数
│       │   └── History.js         # 撤销/重做历史
│       └── effects/        # 特效
│           └── ParticleSystem.js  # 粒子系统
├── data/
│   └── json/               # JSON 数据文件
│       ├── maps_list.json
│       └── pixel_map_data.json
└── README.md               # 项目说明文档
```

## 🏗️ 架构设计

### 模块化设计

项目采用 ES6 模块化设计，将功能拆分为以下模块：

#### 工具类 (utils/)
- **Algorithms.js**: 路径计算算法（Bresenham算法、对角线路径计算）
- **Helpers.js**: 通用辅助函数（ID生成、颜色转换、坐标转换等）
- **Storage.js**: 数据持久化（localStorage 封装）
- **History.js**: 编辑历史管理（撤销/重做）

#### 核心模块 (core/)
- **MapManager.js**: 地图管理（加载、保存、删除、切换）
- **GridRenderer.js**: 网格渲染（主网格、小地图、放大镜）
- **Editor.js**: 像素地图编辑器
- **PathCalculator.js**: 路径计算和路径选择
- **ProgressTracker.js**: 进度跟踪和状态管理

#### UI组件 (ui/)
- **Navigation.js**: 顶部导航栏
- **MapList.js**: 左侧地图列表
- **Toolbar.js**: 编辑工具栏
- **ControlPanel.js**: 右侧控制面板
- **Magnifier.js**: 放大镜功能
- **ColorPalette.js**: 调色板组件

#### 特效 (effects/)
- **ParticleSystem.js**: 粒子背景系统

## 🚀 使用方法

### 启动应用

1. 确保所有数据文件在 `data/json/` 目录下
2. 在浏览器中打开 `index.html`
3. 或使用本地服务器（推荐）：
   ```bash
   # Python 3
   cd apps/PixelKnit
   python -m http.server 8000
   # 然后访问 http://localhost:8000
   ```

## 📝 重构进度

### ✅ 已完成
- [x] 项目结构设计
- [x] 工具类模块（Algorithms, Helpers, Storage, History）
- [x] 粒子系统模块
- [x] 基础 HTML 结构
- [x] 样式文件迁移

### 🚧 进行中
- [ ] 核心模块拆分（MapManager, GridRenderer, Editor等）
- [ ] UI组件拆分（Navigation, MapList, Toolbar等）
- [ ] 主入口文件完善

### 📋 待完成
- [ ] 完整功能迁移
- [ ] 模块间通信优化
- [ ] 性能优化
- [ ] 单元测试

## 🔧 技术栈

- **前端**: HTML5, CSS3, JavaScript (ES6+)
- **模块化**: ES6 Modules
- **Canvas API**: 用于网格渲染和交互
- **localStorage**: 客户端数据持久化
- **Bresenham 算法**: 直线路径计算

## 📄 许可证

MIT License

---

**注意**: 当前版本正在重构中，部分功能可能仍在迁移过程中。完整的模块化架构将在后续版本中完成。

